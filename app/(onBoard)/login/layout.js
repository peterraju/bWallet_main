export const metadata = {
  title: "B-Wallet | Login",
  description: "By Bankless DAO",
};

export default function LoginLayout({ children }) {
  return (
    <section className="feather-bg flex h-screen w-screen">{children}</section>
  );
}
